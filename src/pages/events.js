import * as React from "react"
import { PageLayout } from '../layouts';
import { Link } from 'gatsby';

const IndexPage = () => {
    return <PageLayout>
        <h1>Upcoming Events</h1>
        <p>There are now upcoming events at this time.</p>
    </PageLayout>
}

export default IndexPage
